# 작업 로그 — 연구비 규정 corpus 구축 (2026-08-03)

퇴근 전 중간 저장. 이어서 할 때 이 문서 하단 "다음 단계"부터 진행.

## 배경 / 목표

교수님 요구사항 (전체 목록은 메모리 `project-pnu-chatbot-professor-requirements` 참고):

- 비교 대상 챗봇 **https://yunju.work/grant-rules/** (부산대 산학협력단 연구비 규정
  판정·지식검색 v2.1)보다 성능 좋게 구현할 것
- 그 사이트가 공개한 **53문항 Q&A(연구비관리부 원문 질문+기준답안)**를 채점표로 사용
- 우리 기존 인덱스(2,247 문서)에는 연구비·여비 문서가 0건 → corpus부터 구축 필요
- 상대 원본 자료실(Google Drive)은 권한 차단 → **공식 출처에서 재수집**으로 결정

## 오늘 완료한 것

### 1. 평가셋 확보
- `config/grant-rules-answer-eval.jsonl` — 53문항 (질문+기준답안+출처 섹션+수집 메타)
  - 출처 API: `https://yunju.work/grant-rules/api/question-set`
  - 문항 분포: 국연법 15 / 공무원 여비규정 8 / 부산대 세부지침 5 / 수과원 4 /
    NRF 인문사회 3 / 식약처 3 / 극지·중기부 각 2 / 기타 단일 문항들
- `downloads/grant-rules-drive-20260803/regulations-manifest.json` — 상대 챗봇의
  규정 40개 노드 + 관계 그래프(직접적용/상위근거/보충/개정이력). 상대의 "관계 지도"
  구조를 그대로 확보한 것. 나중에 검색 라우팅·리랭킹에 재활용 가능

### 2. 문서 수집 → `downloads/grant-rules-official-20260803/`
| 출처 | 내용 | 파일 |
|---|---|---|
| law.go.kr 법령 | 국연법·시행령·시행규칙, 공무원 여비 규정, 청소년성보호법+시행령 | 렌더링 HTML/TXT + 별표 HWP 27건 |
| law.go.kr 행정규칙 | **국가연구개발사업 연구개발비 사용 기준**(과기부 고시, 핵심), 산업기술혁신사업 공통 운영요령 | 본문 + 별표 46건(뷰어 XML 텍스트 복원) |
| 부산대 산학협력단 | **규정집 전체 50건** (연구비 관리 세부 지침 2026.4.30 포함) | HWP 50 + manifest.json |
| 중기부 | 기술개발 지원사업 관리지침 2025 (본문+공통서식) | HWP 2 |
| 식약처 | 연구개발비관리지침 (2023.9.22) | PDF 1 |
| 수과원 | 2026년 연구용역비 산정·정산 기준 (※상대는 2025판, 단가 차이 유의) | HWPX 1 |
| NRF/교육부 | 인문사회 학술연구지원사업 종합계획 2024·2026 (별표1=사업비 사용기준) | PDF 2 |

수집 기법 메모 (재수집 시 참고):
- law.go.kr: 친화URL → iframe(`lsInfoP.do`) → 헤드리스 렌더링(gstack browse).
  별표 첨부는 `flDownload.do?flSeq=…&bylClsCd=…`, 행정규칙 별표는
  `bylInfoDiv('<id>')` 실행 후 `viewer/BYL/...//thumbnailxml/<p>.xml` 좌표 복원.
  스크립트: 스크래치 `collect_law.sh` (세션 종료로 소실 시 이 문서 참고해 재작성)
- 산단 규정집: `sanhak.pusan.ac.kr/sanhak/board/16`, ASP.NET이라 curl 포스트백은
  CSRF 차단. **headless 실제 클릭**(`browse click "button[onclick*=\"'17', 'A'\"]"`)
  → 모달의 `a[download]` href(`/upload/...`)를 curl로 다운로드
- mfds.go.kr은 헤드리스 차단 → 대학 산단 미러(동아대 research.donga.ac.kr, gnuboard
  nonce는 페이지 fetch와 같은 세션에서 즉시 사용해야 함)

### 3. 파싱·인덱스 (완료)
- 스테이징: `downloads/grant-rules-official-20260803/content/<기관>/` 8개 기관 97파일
  (법령 렌더 텍스트는 `.txt` 미지원이라 최소 HTML로 래핑)
- 파싱: cascade 프로필, run `20260803-grant-rules-cascade-v1` —
  **97/97 성공**, 블록 102,877 / 표 1,270 / 청크 3,511
- 인덱스: `processed/index/grant-rules-20260803.sqlite` —
  97 docs / 3,483 chunks (suspect 포함, 28청크 제외)

### 4. 부수 작업
- 권한 프롬프트 절감: `~/.claude/settings.json`에 curl·browse·Notion/Drive 검색
  allowlist 추가
- 메모리 저장: `project-pnu-chatbot-professor-requirements` (교수님 요구 7건+상대 분석)

## 미수집 (Task #5 백필, 6문항 해당)
- 극지연구소 2024 위탁연구비 세부기준 (2문항)
- 한국가스공사 정산기준 / 한전 전력연구원 여비 기준 / 창의재단 컨설팅 가이드라인 /
  환경부 학술연구 용역비 세부 계상기준 (각 1문항)
- 참고: NRF 사이트/각 기관 공고 첨부에 있음. 평가 결과에서 해당 문항 실패 확인 후 수집

## 다음 단계 (여기부터 재개)

1. **53문항 검색 평가** — 새 인덱스에 대해 Hit@k 측정
   - `config/grant-rules-answer-eval.jsonl`의 `section` 필드를 기대 문서로 매핑
     (예: "공무원 여비규정" → 국가법령/공무원 여비 규정*, "부산대학교 연구비 관리에
     관한 세부 지침" → 부산대…세부 지침)
   - 검색: `.parser-tools/venvs/core/bin/python scripts/bm25_search.py search
     --index processed/index/grant-rules-20260803.sqlite --top-k 5 --json "<질문>"`
   - 기존 24문항 평가 스크립트(`scripts/evaluate_pnu_retrieval.py`) 형식 참고
2. 실패 문항 분석 → 필요시 Task #5 백필 수집
3. **답변 생성 평가**: search_api에 새 인덱스 연결(`--index` 인자), 53문항 질문 →
   생성 답변을 기준답안과 대조(LLM 채점 or 키워드 루브릭). 상대 챗봇 답변과 비교표 작성
4. 기존 인덱스와 **통합 인덱스** 빌드 (bm25_search.py build는 --chunks 반복 지정 가능)
5. (별도 트랙) 커밋 전 learned dense 작업과 섞이지 않게 브랜치 정리 —
   현재 브랜치 `codex/rag-v2-parser-pipeline`에 커밋 안 된 learned dense 수정
   + 오늘 추가된 untracked 파일들이 공존 중. **커밋 시 새 브랜치 분리 권장**
   (예: `feat/grant-rules-corpus`)

## 저녁 세션 진행 (같은 날 재개 후)

### 검색 평가 결과 (53문항, BM25)
| 구성 | Hit@5 | MRR |
|---|---|---|
| BM25 단독 | 35/46 (76.1%) | 0.585 |
| + 채점 매핑 보정(복수 출처 인정) | 38/46 (82.6%) | 0.621 |
| + **기관 라우팅** (`scripts/rag/grant_router.py`) | **45/46 (97.8%)** | **0.917** |

- 라우터: 질의에 기관 명시 → 해당 기관 고정 / 여비·출장 질의 → 국가법령+과기부 /
  기본 → 공통기준+부산대, 대학 언급 없으면 공통기준·자체규정 교차 배치
- 평가: `scripts/evaluate_grant_retrieval.py --routing`
  (결과 `processed/eval/20260803-grant-rules-bm25-routed-v3.json`)
- 잔여 실패 1건(grant_050, "신청 절차"→제·개정 절차 지침에 오매칭)은 BM25 어휘
  한계 — learned dense(KURE) 하이브리드로 해결 예정. 라우터 과적합은 중단
- 백필 5건(Task 5)은 전부 로그인 포털·비색인 첨부라 보류. 현실 대안 = Drive
  액세스 요청 승인 or 기관 문의. uncovered 7문항으로 집계 중

### 답변 생성 평가 (Task 8, 완료)
- `scripts/evaluate_grant_generation.py`: 라우팅 검색 → Gemini 생성 →
  Gemini judge 채점(0~2점, 기준답안의 수치·조건·절차 포함 여부).
  append 방식이라 중단돼도 `--out` 같은 파일로 재실행하면 이어짐
- v1 (diverse top5, temp 0.2): 전체 평균 0.40 —
  `processed/eval/20260803-grant-generation.jsonl`
- **v2 (하이브리드 컨텍스트: diverse 5 + 상위 3문서 추가 청크 각 2, temp 0)**:
  covered 46문항 **평균 0.67 | 2점 13 / 1점 5 / 0점 28** —
  `processed/eval/20260803-grant-generation-v2.jsonl`
- **핵심 발견: covered 0점 28건 중 27건이 "확인할 수 없다"형 정직한 무응답,
  오답(환각)은 단 1건.** 근거 없는 답을 지어내지 않는다는 신뢰도 스토리 확보
- 무응답 집중 구간 = 여비 단가류 질문. 원인 확인됨: 별표2(국내 여비 지급표)는
  금액까지 정상 파싱·색인돼 있으나, 표 청크 어휘와 질문 어휘가 달라 BM25가
  못 끌어옴 (전형적 어휘 갭)

### 다음 개선 레버 (우선순위순)
1. **learned dense(KURE) 하이브리드를 이 인덱스에 적용** — 어휘 갭(여비 지급표,
   grant_001 카페트, grant_050 신청절차)이 전부 이 부류. 기존 미커밋 작업과 합류
2. 별표 문서 제목 강화(파일명 opaque seq → "[별표2] 국내 여비 지급표" 식) 후
   재파싱 — BM25만으로도 개선 여지
3. 생성 top-k/컨텍스트 예산 튜닝, 무응답 시 스코프 확장 재검색(2-pass)
4. 상대 챗봇 53문항 라이브 응답 수집 → 같은 judge로 채점해 직접 비교표
   (**상대 서비스에 53회 질의 = 비용·부하 발생, 사용자 승인 필요. 미실행**)

## 팀원 작업 분담 계획 (2026-08-04 논의, 비개발 위주)

팀원에게 맡길 작업 후보 — 전부 중간평가 3축(계획 충실도·실험 진행·결과물)에 기여:

1. **평가셋 제작** ⭐ — 기관별(금감원·한은·거래소·예탁원·KISA·KIOST) 역할 페르소나 ×
   질문 × 문서 기반 정답 세트. 인당 기관 1~2개, 20~30문항. 스프레드시트 템플릿 →
   JSONL 변환은 내가. 페르소나 정의(부산대 학생/행정원/기관 실무자 등)도 포함
   — 교수님 "역할 빙의 채점 + 자체 벤치마크 제작 허용" 요구에 직결
2. **사람 채점 + LLM judge 검증** ⭐ — 53문항 중 20개 샘플 2인 독립 채점 →
   LLM 채점과 일치율(kappa). 상대 챗봇 vs 우리 답변 블라인드 A/B 평가
3. **문서 수집** — 백필 5건(NTIS·창의재단 PMS는 로그인하면 가능), yunju.work
   Drive 액세스 요청, KINS(원자력안전기술원) 문서 수집, 파싱 품질 육안 검수
4. **배경조사** ⭐ — RAG 평가 벤치마크 논문(RAGAS 등), 상대 챗봇 기능 분석,
   프롬프트 오염 방지 사례 조사 — 7/22 교수님 "조사 미흡" 피드백 해소
5. **프롬프트 오염 테스트 시나리오 작성** — 오염 문서 시나리오 10~20개 (실행은 내가)

추천 배분(3인): A=평가셋+페르소나 / B=채점·블라인드비교·검수 / C=조사·시나리오·수집.
다음 액션 후보: 팀원 전달용 작업 지시서 + 평가셋 템플릿 제작 (사용자 요청 대기)

## 2026-08-04 세션 — learned dense 하이브리드 적용 (레버 1)

### 브랜치 정리 (선행 작업)
- `feat/learned-dense-hybrid` (b6669b7): dense 검색 코드 + search_api/UI 연동
- `feat/grant-rules-corpus` (d8e8c70): 연구비 평가 트랙, 위 브랜치 위에 스택
- `diagrams/`는 7/28 데모 커밋에서 누락된 고아 파일이라 어느 쪽도 아니어서 untracked 유지

### 인덱스 빌드
grant corpus(3,483 chunks)에 대해 두 모델 모두 빌드, 각 27초 / `verified: true`:
`processed/index/learned-dense/grant-rules-20260803/{kure-v1,snowflake-arctic-l-v2-ko}/`

### 검색 평가 (53문항, 라우팅 적용, top-5)
| 구성 | Hit@5 | MRR |
|---|---|---|
| **BM25 (어제 baseline 재현)** | **45/46 (97.8%)** | **0.9167** |
| KURE dense 단독 | 41/46 (89.1%) | 0.8377 |
| KURE 하이브리드(RRF) | 44/46 (95.7%) | 0.8967 |
| Snowflake dense 단독 | 44/46 (95.7%) | 0.8986 |
| Snowflake 하이브리드(RRF) | 44/46 (95.7%) | 0.8967 |

> ⚠️ **이 표만 보고 "dense는 문서 검색에 약하다"고 결론내면 안 된다.** BM25 경로
> (`search_index`)에는 마지막에 `select_document_diverse_results(max_chunks_per_document=2)`
> 문서 다양화가 붙어 있는데 dense 경로(`HybridRetriever`)에는 없다. 문서 단위 Hit@5는
> 바로 그 다양화가 직접 최적화하는 지표라, **BM25에만 핸디캡이 붙은 불공정 비교다.**
> 아래 원인 분석 참고. 공정 재측정 전까지 이 표는 확정 결론이 아니다.

표면적으로는 dense가 후퇴다. BM25가 이미 45/46이라 헤드룸이 없고, dense는
grant_011/018/021/023/028을 1위에서 miss로 떨어뜨린다.
단, **어제 유일한 miss였던 grant_050("신청 절차")은 모든 dense 구성에서 해결**
(miss → 2위, 하이브리드는 4위). 예측했던 어휘 갭 사례가 맞았다.

### 핵심 진단 — 청크 단위 근거 커버리지 (이 작업의 실제 목적)
문서 단위 Hit@5는 **어제 무응답을 유발한 표 청크가 컨텍스트에 들어왔는지**를 보지
못한다. 생성 0점 35문항 중 기준답안에 수치가 있는 19문항에 대해, 라우팅 top-8
컨텍스트 안에 기준답안의 수치(금액·비율·기간)가 실제로 등장하는지 측정했다.

| 구성 | 근거 수치가 컨텍스트에 등장 | baseline 대비 |
|---|---|---|
| BM25 | 10/19 | — |
| **KURE dense** | **17/19** | +7, **잃은 문항 0** |
| Snowflake dense | 16/19 | +6, 잃은 문항 0 |
| Snowflake 하이브리드 | 16/19 | +6, 잃은 문항 0 |
| KURE 하이브리드 | 15/19 | +5, 잃은 문항 0 |

**두 지표가 정반대를 가리킨다.** BM25는 올바른 *문서*를 찾지만 그 안의 올바른
*청크*(금액이 든 별표)를 못 집는다. dense는 그 반대다. 어느 구성도 BM25가 이미
확보한 근거를 잃지 않았다(strictly dominant) — 순수 추가 이득이다.
새로 근거를 확보한 문항: grant_023, 028, 032, 034, 036, 045, 046.

grant_038·051은 전 구성에서 여전히 0 — 별도 원인(파싱 누락 또는 미수집) 확인 필요.

### 원인 분석 — dense는 왜 문서 단위에서 밀렸나

재현: `scripts/diagnose_grant_retrieval.py` (결과 `processed/eval/20260804-grant-diagnosis.json`)

| mode | 근거 | 중앙길이 | 짧은청크 | 문서수 | 기관수 | 폴백 |
|---|---|---|---|---|---|---|
| bm25 | 10/19 | 1668자 | 3.6% | 3.49 | 2.26 | 17 |
| kure-dense | **17/19** | 1259자 | **18.1%** | 2.89 | 1.79 | 9 |
| kure-hybrid | 15/19 | 1738자 | 3.4% | 3.43 | 2.23 | 8 |
| snow-dense | 16/19 | 1729자 | 7.9% | 2.94 | 1.92 | 4 |
| snow-hybrid | 16/19 | 1742자 | 1.9% | 3.40 | 2.28 | 9 |

**1. 짧은 청크 편향 (가장 큰 원인)**
인덱스의 41.4%가 100자 미만(표 셀·조문 제목·별표 헤더 파편). BM25는 용어가 실제로
누적돼야 점수가 나와 3.6%만 뽑는데 KURE dense는 18.1%로 5배다. **코사인 유사도에는
길이 사전확률이 아예 없어서**, 짧은 청크의 임베딩이 소수 토큰에 지배되며 짧은 질의와
우연히 높은 유사도를 얻는다. grant_028에서 KURE가 1위로 올린 건 12자 청크,
grant_011은 17자 청크였다.
→ Snowflake가 KURE보다 문서 단위에서 나았던 것(44 vs 41)도 이걸로 설명된다.
Snowflake는 `query: ` 프리픽스를 쓰는 비대칭(질의↔문서) 학습 모델이라 짧은 질의를
긴 문서에 매칭하도록 훈련됐고, 짧은 청크 과다 검색이 절반 이하(7.9%)다.

**2. 라우터 폴백이 억제됨 — 라우터가 BM25에 맞춰 튜닝돼 있었다**
`filter_hits`에는 "스코프 내 결과가 top_k보다 적으면 원본으로 보충"하는 탈출구가 있다.
BM25는 어휘가 희소해 17문항에서 이게 발동하고, 라우터가 스코프를 잘못 잡아도 구제된다.
dense는 의미적으로 그럴듯한 청크로 항상 할당량을 채워 그 구제를 못 받는다(9, snow는 4).
grant_028이 정확한 사례: 질의에 "한국연구재단"이 명시돼 있으나 라우터 규칙은 "인문사회"
키워드만 봐서 기본 스코프로 보낸다. BM25는 스코프를 못 채워 NRF 문서로 보충돼 1위를
맞췄고, dense는 부산대 문서로 5칸을 채워 miss가 됐다.
→ **어제 97.8%를 만든 라우터는 BM25의 희소성에 의존한 수치였다.** 어제 적어둔
"라우터 과적합은 중단"이 여기서 드러난다.

**3. 문서 다양화 비대칭 — 비교 자체가 불공정했다**
`search_index`만 `select_document_diverse_results(max_chunks_per_document=2)`를 적용한다.
top-5 안의 서로 다른 문서 수가 BM25 3.49 vs dense 2.89. 한 문서에서 2개까지만 뽑으면
5칸에 더 많은 문서가 들어가 정답 문서가 낄 확률이 올라간다. 문서 단위 Hit@5는 이
다양화가 직접 최적화하는 지표다.

**4. 고유명사는 압축되고 주제만 남는다**
corpus가 8개 기관의 유사 규정 모음이라 의미적으로 near-duplicate다. 기관을 구별하는
신호는 기관명·사업명·조문번호 같은 고유명사인데, 어휘 매칭의 강점이자 임베딩이 뭉개는
정보다. grant_018(양자컴퓨팅 과제의 서버 메모리)에서 dense는 부산대 물품구매·연구장비
지침을 가져왔다 — "장비 구매"라는 **주제는 맞췄지만 권한 계층**(과기부 국가기준 vs
교내 절차)을 구분하지 못했다.

### 결론 및 다음 레버
- 생성 품질을 좌우하는 건 청크 단위 근거 커버리지이므로 **dense를 도입할 가치가 있다.**
  다만 dense 단독은 문서 정밀도를 잃어 오문서 인용 위험이 있다
- 현행 RRF 하이브리드는 두 지표 모두에서 어중간하다(문서 44/46, 근거 15/19)
- 임베딩 모델은 근거 커버리지 기준 KURE(17/19) > Snowflake(16/19), 문서 정밀도는
  Snowflake 우위. 아래 1번을 끝낸 뒤 다시 비교할 것

**수정 우선순위 (원인 분석 후 재조정, 위 4개 원인에 대응)**
1. **dense 경로에 문서 다양화 + 최소 길이 필터 적용** — 기존
   `select_document_diverse_results`를 그대로 재사용하면 되므로 거의 공짜고,
   원인 1·3을 동시에 잡는다. **dense의 실제 성능을 처음으로 공정하게 재는 것**이라
   이걸 먼저 하지 않으면 위 문서 단위 표는 계속 해석 불가다
2. **짧은 청크 처리** — 100자 미만 41.4%를 상위 조문에 병합하거나 parent chunk로
   확장. 인덱스 품질 자체 개선이라 BM25에도 이득 (원인 1의 근본 해결)
3. **라우터에 기관명 명시 규칙 추가** — grant_028은 `EXPLICIT_INSTITUTIONS`에
   "한국연구재단" 한 줄이면 해결 (원인 2)
4. **생성 컨텍스트를 "BM25로 문서 선택 + dense로 청크 보강"으로 구성** —
   `evaluate_grant_generation.py`의 컨텍스트 조립부(diverse 5 + 상위 문서 추가 청크)
   에서 추가 청크를 BM25 raw 대신 dense로 뽑는 방식. 문서 권한 판단은 BM25,
   청크 내부 근거 탐색은 dense로 나누므로 **원인 1·2·4를 구조적으로 우회**한다

### 결정 기록 (8/4)

앞으로 세션마다 이 형식으로 남긴다: 상황 → 검토한 대안 → 선택 → 근거 → 기각 이유.
결과 수치만 남기면 나중에 왜 그렇게 했는지 재구성이 안 되고 기각한 대안을 다시 검토하게 된다.

**D1. 브랜치를 2개로 분리 (learned-dense / grant-corpus)**
- 상황: 한 브랜치에 dense 미커밋 변경(10파일 +1206줄)과 grant untracked 파일이 공존
- 판단 근거: 수정 파일들을 열어보니 경계가 명확했다. `generators.py`의 Gemini 모델
  오버라이드는 UI 셀렉터용이고, grant 평가 스크립트는 `generate(requested="gemini")`만
  써서 의존이 없다 → 두 트랙이 실제로 독립
- grant 브랜치를 dense 브랜치 **위에 스택**한 이유: 다음 레버가 두 트랙을 합치는
  작업이라 어차피 dense 코드가 필요하다. 나란한 브랜치로 두면 나중에 병합 비용만 든다
- `diagrams/`는 커밋하지 않음: 7/28 데모 커밋(3a32061)에서 누락된 고아 파일이라
  어느 트랙에도 속하지 않는다. 억지로 끼워넣으면 이력이 더러워진다

**D2. `grant_retrieval.py`를 `rag/`가 아니라 `scripts/`에 배치 (계획과 다름)**
- 계획서에는 `scripts/rag/grant_retrieval.py`로 적었으나 구현 중 변경
- 이유: `rag/` 패키지의 어떤 모듈도 `bm25_search`를 import하지 않는다. BM25 레인은
  `search_api`가 주입하는 구조 = 의도된 계층 분리. 여기에 넣으면 그 규칙이 깨진다
- 기각한 대안: `search_api`에서 `learned_dense_artifact_path`를 import해 경로 해석
  재사용 → HTTP 서버 모듈(2,900줄)을 평가 스크립트에 끌어들이게 되어 기각.
  대신 `--dense-artifact`로 경로를 직접 받는다

**D3. 검색 평가만 먼저, 생성 평가는 보류**
- 이유: 검색 평가는 LLM이 필요 없어 비용 0. 생성 평가는 53문항 × (생성+judge)로
  어제 쓴 비용이 다시 든다. 검색이 실제로 개선됐는지 먼저 보는 게 순서
- **결과적으로 옳은 판단이었다.** 문서 단위 지표가 후퇴로 나왔고 원인 분석이 필요했다.
  바로 생성 평가로 갔으면 원인 모른 채 점수만 얻었을 것

**D4. Snowflake 아티팩트 추가 빌드**
- 40초 / 비용 0인데 "왜 이 임베딩 모델인가"를 실험 기록으로 남길 수 있어서
- **결과적으로 원인 분석의 핵심 증거가 됐다.** 두 모델의 짧은 청크 비율 차이
  (18.1% vs 7.9%)가 비대칭 학습 가설을 뒷받침했다. 한 모델만 봤으면 못 봤을 것

**D5. 생성 평가는 로컬 gemma, judge는 Gemini 유지**
- 로컬 모델: `mlx-community/gemma-4-26b-a4b-it-4bit` (사용자 선택, EXAONE 제외)
- judge를 Gemini로 두는 이유: 어제 0.67이 Gemini judge 기준이라 **채점자를 바꾸면
  비교 자체가 불가**. 토큰의 대부분은 생성(청크 8개 컨텍스트)이고 judge는 짧아서,
  생성만 로컬로 옮겨도 비용은 거의 사라진다
- 유의: 생성기를 바꾸면 검색 개선분과 생성기 교체분이 섞인다 →
  "옛 검색 + 로컬 생성" baseline 1회가 추가로 필요

**D6. 청크 단위 근거 커버리지를 별도 지표로 도입**
- 상황: 문서 단위 Hit@5가 45/46이라 헤드룸이 없는데, 생성은 0.67에 머물렀다.
  두 숫자가 안 맞으면 지표가 목표를 대변하지 못하는 것
- 그래서 "기준답안의 수치가 검색 컨텍스트에 실제로 등장하는가"를 직접 쟀다
- **이 지표가 없었으면 dense를 폐기했을 것이다.** 문서 단위로는 후퇴로 보였다

### 이번 세션 코드 변경
- `scripts/grant_retrieval.py` (신규): bm25/dense/hybrid를 `(query, top_k) -> list[dict]`
  하나로 감싼 어댑터. `rag` 패키지는 BM25에 의존하지 않는 재사용 계층이라 글루 코드는
  `search_api`와 같은 스크립트 계층에 뒀다
- `scripts/evaluate_grant_retrieval.py`: `--retrieval-mode`, `--dense-artifact` 추가
- `tests/test_grant_retrieval.py` (신규): 세 모드 배선 검증. 전체 219 테스트 통과
- `scripts/diagnose_grant_retrieval.py` (신규): 근거 커버리지·청크 길이·문서 다양성·
  라우터 폴백을 한 번에 측정. Hit@k가 못 보는 실패 원인을 재현 가능하게 남기기 위함
- 평가 결과: `processed/eval/20260804-grant-*.json` 5건 + `-diagnosis.json`

### 생성 평가 (미실행, 결정 사항 기록)
- 로컬 모델 `mlx-community/gemma-4-26b-a4b-it-4bit`로 진행하기로 결정 (EXAONE 제외)
- 착수 시: `evaluate_grant_generation.py`는 `generators.generate`를 직접 호출해
  MLX 서버가 뜨지 않으므로, `scripts/local_model_runtime.py`의
  `build_local_model_runtime()`을 재사용해 `runtime.generation(model)`으로 감싸야 함
  (현재 `managed_mlx` 기동은 `search_api`만 소유)
- judge는 Gemini 유지 권장 — 어제 0.67이 Gemini judge 기준이라 채점자를 바꾸면 비교 불가.
  생성기 교체분도 있으므로 "옛 검색 + 로컬 생성" baseline 1회가 추가로 필요

## 2026-08-04 오후 — 공정 재측정, BM25 길이 분석, 그리고 진짜 원인

### 1. 공정 재측정 (수정 우선순위 1번 실행)

dense 경로에도 BM25와 같은 문서 다양화를 적용했다. `grant_retrieval.build_searcher`에
`diversify`(기본 True) / `min_chars` 옵션 추가, 두 레인이 같은 후처리를 타게 통일.
**bm25 모드는 문항별 rank까지 기존 baseline과 완전히 동일**하게 재현됨을 확인(리팩터 안전).

| 구성 | Hit@5 (전 → 후) | 근거 커버리지 (전 → 후) |
|---|---|---|
| BM25 | 45/46 (변화 없음) | 10/19 |
| KURE dense | 41/46 → **44/46** | 17/19 → **15/19** |
| KURE 하이브리드 | 44/46 → **45/46** | 15/19 → **11/19** |
| Snowflake dense | 44/46 (동일) | 16/19 → 15/19 |
| Snowflake 하이브리드 | 44/46 → **45/46** | 16/19 → 13/19 |

**"dense는 문서 단위에서 후퇴"는 상당 부분 측정 아티팩트가 맞았다** — 하이브리드가
BM25와 동률(45/46)이 됐다. 원인 3(다양화 비대칭) 확인 완료.

**그러나 새 상충이 드러났다: 다양화는 근거 커버리지를 떨어뜨린다.**
`max_chunks_per_document=2`가 한 문서에서 2청크까지만 허용하는데, 여비 답변은
**같은 문서 안의 조문 + 별표를 함께** 봐야 한다. 두 지표가 구조적으로 상충한다.
→ 다양화는 **dense를 공정하게 평가하는 용도**로만 쓰고 실제 파이프라인에는 넣지 말 것.

`min_chars=100`은 문서 단위 지표를 **전혀 바꾸지 않았다**(다양화가 이미 파편을 밀어냄).
게다가 정답인 181자 여비 지급표가 필터에 걸릴 뻔한 쪽이라 **길이는 틀린 도구**였다.
짧은 청크의 정체는 길이가 아니라 패턴(`210mm×297mm` 용지 규격 boilerplate, 목차 항목)이다.

### 2. BM25 길이 편향 검증 (사용자 질문에서 출발)

"BM25는 길수록 점수가 오르는 구조 아니냐"는 질문을 직접 측정했다. SQLite FTS5
`bm25()` 기본 파라미터(k1=1.2, **b=0.75**) 사용 중이고, 평가 경로는 리랭커를 안 탄다.

**실험 A — 같은 단어를 정확히 1회만 포함한 청크만 비교(TF 고정, 길이만 변수):**

| 단어 | 짧은 1/3 | 긴 1/3 | 유리한 쪽 |
|---|---|---|---|
| 여비 | 15자 → **-5.53** | 1,163자 → -2.92 | 짧은 쪽 |
| 간접비 | 57자 → **-4.02** | 1,786자 → -1.39 | 짧은 쪽 |

(FTS5는 음수, 작을수록 좋은 매치) **길이 정규화가 강하게 작동 중**이며 조건이 같으면
짧은 쪽이 압도적으로 유리하다. "길수록 오른다"는 정규화 없는 순수 TF의 성질이다.

**실험 B — 그런데 실제 검색 결과 중앙값은 1,668자다. 왜?**

| 질의 용어 수 | 검색된 청크 중앙 길이 |
|---|---|
| 1개 | 190자 |
| 3개 | 603자 |
| 12.2개(실제 질의) | **1,728자** |

**BM25는 매칭된 용어별 기여를 합산**한다. 긴 청크는 질의 용어를 더 많이 담을 확률이
높고, 길이 정규화가 용어 하나하나를 깎아도 "3개 매칭 vs 1개 매칭"을 뒤집지 못한다.
실험 C: 상위 5개는 질의 용어 평균 2.59개, 하위 5개는 1.61개를 담고 있으며
**길이는 오히려 하위가 더 길다**(1,716 vs 1,649자). 순위를 정하는 건 길이가 아니라
**용어 커버리지**다.

→ 원인 1의 정확한 재기술: "dense의 길이 편향"이 아니라
**"BM25의 용어 커버리지 편향이 정보 밀도 높은 짧은 표를 장황한 조문에 밀리게 한다"**.

### 3. 진짜 원인 — `section_path` (이번 세션 최대 발견)

청킹(`core/chunking.py`)은 **구조 기반**이다: 일반 블록은 같은 `section_path` 안에서만
병합되고, 표는 독립 청크, `table_cell`은 색인 제외. **즉 청크 경계 = 파서가 뱉는
`section_path`**다. 경로가 `null`이면 끊을 기준이 없어 `max_chars=1800`까지 뭉친다.

`section_path`는 제목 스택이다(`adapters/native.py`): hN을 만나면 `headings[:N-1]`로
자르고 현재 제목을 얹은 뒤, 이후 본문 블록에 그 경로를 복사한다.

**PNU corpus 프로필별 구조 산출 능력:**

| 프로필 | section_path 확보율 | 청크 수 | 중앙 길이 | 총 글자 |
|---|---|---|---|---|
| baseline | 78.7% | 51,161 | 66자 | 14.53M |
| **challenger** | **93.8%** | 43,566 | 79자 | 13.09M |
| cascade | 85.0% | 44,520 | 77자 | 13.72M |

challenger는 텍스트를 가장 적게 뽑는데 **구조는 가장 잘 잡는다**. HWP에서 baseline/
cascade는 hwplib(자바)를 먼저 쓰고 challenger는 unhwp 계열을 먼저 쓰는 차이다.

**사용자의 관찰 재현**: "휴학은 항상 challenger > baseline > cascade" (사람이 답변까지
보고 판단). 질의 "군휴학 복학 절차"로 세 인덱스 검색:

| 프로필 | section_path | 결과 |
|---|---|---|
| challenger | `["나.  복학"]` | 540자, 복학 조항만 온전 → **1위** |
| baseline | `["재외국민…","부 산 대 학 교","목 차","학사제도 안내"]` | 목차 수준이라 사실상 안 끊김 → 교직과정 내용이 1,660자로 → 엉뚱 |
| cascade | `null` | 1,721자 뭉텅이가 용어 커버리지로 정확한 540자 청크를 밀어냄 → 3위 |

**사람 평가 순서가 그대로 재현됐다.** 그리고 7/28 지표에서 challenger는 MRR 꼴찌
(0.842)였다 — 모순이 아니다. **Hit@k/MRR은 "맞는 문서를 찾았나"만 재고, 답변 품질은
"그 안의 청크가 온전한가"가 결정한다.** `section_path` 확보율이 진짜 성능에 더 가까운
지표인데 지금까지 아무도 재지 않았다.

**연구비 corpus에 그대로 적용된다** (cascade, 전체 확보율 **65.9%** — PNU 85.0%보다 낮음):

| 기관 | 청크 | section_path 확보율 |
|---|---|---|
| 과학기술정보통신부 | 60 | 100.0% |
| 국가법령(별표 HWP) | 271 | **51.3%** |
| 부산대학교 산학협력단 | 1,521 | **41.4%** |

그리고 **여비 지급표 청크가 전부 `null`**이다:
```
[948자] sec=null  ■ 공무원 여비 규정 [별표 2] 국내 여비 지급표(제10조…)
[181자] sec=null  구 분 책임연구원 연구원이하 / 일비 25,000 25,000 / 식비 25,000…
```
정답 청크에 숫자만 있고 **"여비"라는 단어가 없다.** 질의와 겹치는 어휘가 0이니 BM25가
찾을 방법이 없다. 경로가 `["공무원 여비 규정","[별표 2] 국내 여비 지급표"]`로 잡혔다면
정상 검색됐을 것이다.

→ **어제부터 "BM25 어휘 갭"이라 부르며 dense로 우회하려던 문제가, 실은 파싱 단계에서
구조를 잃어서 생긴 문제였다.** dense 튜닝은 증상 완화였다.

### 결정 기록 (8/4 오후)

**D7. 다양화를 두 레인에 통일 — 단, 평가 전용**
- 근거: 문서 단위 비교의 불공정을 제거해야 dense의 실제 성능을 알 수 있다
- 결과: 아티팩트 가설 확인(하이브리드 45/46). 그러나 근거 커버리지가 떨어져
  **파이프라인 기본값으로는 쓰지 않기로** 결정
- 기각: `min_chars` 길이 필터 — 효과 0이고 정답 표 청크를 위협. 짧은 청크는 길이가
  아니라 패턴으로 걸러야 함

**D8. BM25 길이 편향 가설을 기각하고 용어 커버리지로 재정의**
- 사용자 제기 가설("길수록 점수 상승")을 TF 고정 실험으로 반증(짧은 쪽이 -5.53 vs -2.92)
- 대신 실제 현상은 재현됐고(1개 용어 190자 → 12개 용어 1,728자) 메커니즘이 다름을 확인
- 이 재정의가 없었으면 길이 필터/길이 페널티라는 **틀린 처방**으로 갔을 것

**D9. 병목을 검색 문제에서 파싱 문제로 재분류**
- 계기: "파서 3개 중 1개만 돌린 거냐" + "휴학은 challenger가 제일 좋더라"는 사용자 관찰
- 사람 평가가 지표와 어긋난 지점을 파고들어 `section_path`에 도달
- **사람 관찰이 지표보다 먼저 진실을 가리켰다.** 24문항 Hit@k는 이걸 볼 수 없었다

**D10. 연구비 corpus를 baseline·challenger로도 파싱 (실행 중)**
- 어제 cascade만 돌린 상태. run-id `20260804-grant-rules-{baseline,challenger}-v1`
- 어제와 동일 설정(chunk_chars 1800 / overlap 250 / min_chars 20 / expect_korean)
- 검증 가설: challenger가 별표 HWP의 `section_path` 확보율을 올려 근거 커버리지를 높인다

### 다음 단계
1. 세 프로필 인덱스 빌드 → `section_path` 확보율 + Hit@5 + 근거 커버리지 3축 비교
2. **`section_path`를 청크 텍스트에 주입** (재파싱 불필요, 인덱스 재빌드만) —
   `[공무원 여비 규정 > 별표2 국내 여비 지급표] 구 분 책임연구원…` 형태.
   변수가 하나라 원인 분리가 되고 가장 쌈
3. 그 뒤에 생성 평가(로컬 gemma + Gemini judge)

## 2026-08-04 저녁 — 파서 3종 비교, 프로필 확정, 주입 가설 기각

연구비 corpus를 baseline·challenger로도 파싱(run-id `20260804-grant-rules-{baseline,challenger}-v1`,
어제와 동일 설정)해 3축으로 비교했다.

### 측정 조건 통일 (중요)

첫 빌드에서 `--allow-suspect`를 빠뜨려 어제 인덱스(A)와 청크 수가 달랐다(3,483 vs 3,435).
**주입 효과를 재려면 그것 말고는 전부 같아야 하므로** 모든 인덱스를 `--allow-suspect`로
통일하고, 주입 전/후를 `git stash`로 분리해 6종을 빌드했다. cascade-off가 3,483으로
어제 인덱스를 정확히 재현하는 것을 확인한 뒤에 비교를 시작했다.

또한 백필 청크 파일을 challenger 런 디렉터리에 두면 게이트가 그 런의 `documents.jsonl`로
검증해 5건을 미상 문서로 제외한다(`inspect_corpus`는 chunks와 같은 디렉터리의
documents.jsonl을 본다). **백필 파일은 원본 런(cascade) 디렉터리에 둬야 한다.**

### 3축 비교 결과

| 프로필 | section_path | 청크 중앙 | Hit@5 | MRR | 근거 커버리지 | 파싱 |
|---|---|---|---|---|---|---|
| baseline | 67.6% | 71자 | 44/46 | 0.895 | 10/19 | 92/97 |
| **challenger** | **76.1%** | 167자 | 45/46 | 0.914 | **12/19** | 92/97 |
| cascade (기존) | 65.9% | 168자 | 45/46 | 0.917 | 10/19 | **97/97** |

- challenger는 **grant_023, grant_046을 새로 확보하고 잃은 문항 0** — strictly dominant
- 기관별로 갈린다: **부산대 산학협력단(HWP) 72.4% vs cascade 41.4%.** unhwp가 hwplib보다
  문단 스타일에서 소제목을 훨씬 잘 살린다
- **국가법령 별표 HWP는 세 프로필 모두 ~50%로 동일** — 법령 별표는 문서 자체에 제목
  구조가 없어 파서가 만들어낼 수 없다
- baseline·challenger는 **같은 5개 문서에서 실패**(산업교육진흥법 계열 3건 + 회계처리규칙
  2건, 전부 `SubprocessError`). cascade 폴백 체인이 이 5건을 복구한다 → 백필로 해결

### `section_path` 주입 가설 — 기각

`make_search_text()`에 institution·file_name·relative_path·source_title은 들어가는데
`section_path`만 빠져 있었다. "문서 안에서 어느 청크인지 구별할 근거가 없다"는 가설로
주입을 시험했다. **결과: 효과 0.**

| | Hit@5 (off → on) | MRR (off → on) | 근거 커버리지 |
|---|---|---|---|
| cascade | 45/46 → 45/46 | 0.9167 → **0.9058** | 10 → 10 |
| baseline | 44/46 → 44/46 | 0.8949 → 0.8949 | 10 → 10 |
| challenger | 45/46 → 45/46 | 0.9138 → 0.9138 | 12 → 12 |

**무효과가 버그 때문이 아님을 확인했다**(중요 — 그냥 "효과 없음"으로 넘기면 안 됨):
- 주입은 정상 작동. 청크의 74.2%에서 토큰이 늘었다(중앙값 +17)
- 50.3%는 **새 단어**도 얻었다(중앙값 10개). 그런데 그 단어가 `목적`, `범위`, `적용`
  같은 **조문 제목의 일반어**라 금액·조건을 묻는 질의와 겹치지 않는다
- 나머지 절반은 **중복 토큰만 증가**했다. section_path의 어휘가 이미 파일명·경로에
  들어 있기 때문. 이 TF 증가가 cascade의 MRR을 떨어뜨린 것으로 보인다(0.9167 → 0.9058)
- 결정적으로 **정작 도움이 필요한 여비 표 청크는 `section_path=None`이라 주입이 닿지
  않는다.** "국내 출장 시 일비는 얼마인가요"에서 금액 청크 순위는 세 인덱스 모두 3위로 동일

→ **변경을 되돌렸다.** 계획에 적어둔 기각 기준("효과가 음수면 되돌린다")대로.

### 채택

`processed/index/grant-rules-20260804.sqlite` = challenger 전체 + cascade 5건 백필,
`--allow-suspect`, 97문서 / 3,395청크. Hit@5 45/46, MRR 0.9138, 근거 12/19.
cascade 대비 MRR이 0.003 낮지만 **근거 커버리지가 2문항 높고 잃은 문항이 없다** —
생성 품질을 좌우하는 건 후자다.

### 결정 기록 (8/4 저녁)

**D11. 파서 프로필을 challenger + cascade 백필로 확정**
- 대안 1: cascade 유지 → 근거 커버리지 10/19로 열세
- 대안 2: challenger 단독 → 5개 문서 손실
- 대안 3(채택): challenger + 그 5건만 cascade에서 백필 → 97문서 전체 유지하며 구조 우위
- 근거: 근거 커버리지 12/19(잃은 문항 0), 부산대 HWP의 section_path 41.4% → 72.4%
- MRR 0.917 → 0.914 하락은 감수. 문서 단위 지표는 이미 천장(45/46)이고 오늘 하루
  이 지표가 목표를 대변하지 못한다는 걸 두 번 확인했다

**D12. section_path 주입 기각 — 다만 "왜 안 되는지"까지 확인하고 기각**
- 무효과를 관측했을 때 바로 접지 않고, 주입이 실제로 인덱스에 반영됐는지부터 검증했다
  (74.2% 토큰 증가, 50.3% 새 단어). 버그였다면 잘못된 기각이 됐을 것
- 근본 이유 둘: (a) 추가되는 어휘가 조문 제목의 일반어라 질의와 안 겹침
  (b) 정작 필요한 표 청크는 section_path가 없음
- **교훈: 구조 정보를 검색에 넣는 것 자체는 방향이 맞지만, 구조가 없는 문서에는
  쓸 수 없다.** 법령 별표는 파서·검색이 아니라 다른 층위에서 풀어야 한다

**D13. 측정 조건 통일을 먼저 하고 비교했다**
- `--allow-suspect` 누락으로 청크 수가 달랐던 것을 발견하고, 비교 전에 6종을 재빌드
- cascade-off가 어제 값을 정확히 재현하는지 확인한 뒤에 진행
- 오전에 다양화 비대칭으로 지표가 뒤집힌 전례가 있어 이번에는 먼저 통제했다

### 다음 단계
1. **레버 4**: 생성 컨텍스트를 "BM25로 문서 선택 + dense로 청크 보강"으로.
   여비 표 청크는 항상 3~4위로 문서 안에는 있으나 top-k 컨텍스트에서 밀린다.
   같은 문서의 형제 청크를 dense로 끌어오면 해결 가능성이 높다
2. 생성 평가(로컬 gemma + Gemini judge)로 0.67 대비 개선폭 측정
3. 국립수산과학원 HWPX는 세 프로필 모두 section_path 0% — 별도 확인 필요

## 2026-08-04 밤 — RAGAS context recall 도입 (우선순위 0)

자체 정규식 지표(근거 커버리지)를 검증된 표준 지표로 교체했다. 자체 지표는 감사 결과
결함이 있었다 — 분모 19에 corpus 미수집 6문항이 섞여 실제 상한이 13이었고, 부분문자열
오탐이 가능하며, 수치가 없는 답변(조건·절차)은 아예 측정 대상에서 빠졌다.

### 환경

`.parser-tools/venvs/ragas`에 격리. `ragas==0.4.3` + **`langchain-community<0.4` 핀 필요** —
langchain-community가 0.4부터 `chat_models.vertexai` 경로를 제거했는데 ragas가 그걸
import한다. 판정 LLM은 Gemini를 **OpenAI 호환 엔드포인트**로 붙였다
(`https://generativelanguage.googleapis.com/v1beta/openai/`). 덕분에 `langchain-google-genai`를
추가로 깔지 않아도 되고, ragas에 이미 딸려오는 `openai` 패키지만 쓴다.

### 구조 — 검색과 채점을 분리

- `scripts/export_grant_contexts.py` (임베딩 venv): 검색 → RAGAS 입력 JSONL
- `scripts/score_grant_ragas.py` (ragas venv): `ContextRecall` 채점

분리한 직접적 이유는 ragas가 별도 venv에 격리돼 sentence-transformers와 같은 환경에
둘 수 없어서다. 부수 효과로 **컨텍스트가 파일로 남아 채점을 다시 돌릴 때 검색을
반복하지 않아도 된다** — 개선안마다 재측정할 것이므로 이게 크다.

### 처리량 문제와 해결

첫 구현은 완전 직렬이라 **35.8초/문항(53문항 32분)**이었다. 원인 둘 다 낭비였다:

1. `--rpm 10`을 걸어놓고 실제로는 분당 1.7회만 씀 — rate limit을 8할 넘게 놀림
2. 폴백이 "모델 A로 3회 재시도(sleep 3+6+9=18초) → 모델 B" 순서라, 기본 모델이
   과부하일 때마다 18초를 버림. 실패는 rate limit(429)이 아니라 **모델 과부하(503)**였다

수정:
- 폴백을 **"라운드 × 모델"**로 뒤집음. 한 라운드에서 모델을 한 바퀴 돌고, 라운드 전체가
  실패했을 때만 백오프. 503은 다른 모델이 즉시 응답하므로 손실이 18초 → 1~2초
- `asyncio.Semaphore` + `gather`로 유한 동시 실행(`--concurrency`, 기본 6)

**결과: 35.8초 → 11.1초/문항 (3.2배), 실효 5.4 RPM(상한 10 이하), 실패 0건.**
`RateLimiter`가 `asyncio.Lock`으로 전역 간격을 강제하므로 동시성을 올려도 한도를
넘지 않는다 — 동시성은 한도를 실제로 채우는 역할만 한다.

### 지표 검증 (중요)

**RAGAS가 우리 병목을 정확히 판별한다**는 것을 먼저 확인했다:
- 금액이 든 표가 컨텍스트에 있으면 `context_recall = 1.0`
- 별표를 가리키기만 하는 조문만 있으면 `0.0`

그리고 **어제 생성 평가와 단조 증가 관계**가 나왔다:

| 어제 생성 judge | 문항 수 | context recall 평균 |
|---|---|---|
| 0점 | 35 | 0.362 |
| 1점 | 5 | 0.543 |
| 2점 | 13 | **0.660** |

즉 context recall이 **생성 품질의 선행 지표**로 작동한다. 앞으로 개선안을 LLM 생성 없이
이 지표만으로 판정해도 되는 근거다.

### challenger 기준선 (채택 인덱스, 53문항)

| | 값 |
|---|---|
| context recall 평균 | **0.4522** |
| 중앙값 | 0.5000 |
| 1.0 (완전 뒷받침) | 16문항 |
| 0.0 (근거 없음) | 21문항 |

### 결정 기록 (8/4 밤)

**D14. 채점 LLM은 로컬이 아니라 Gemini**
- 사용자가 로컬(gemma/qwen) 사용을 검토했으나, 이 작업에는 불리하다고 판단
- 근거: 채점 프롬프트에 top-8 컨텍스트(문항당 약 12,000자 ≈ 6,000토큰)가 들어가
  MLX prefill이 문항당 15~30초. 동시 실행 개선 후 Gemini(11.1초)보다 느리다
- 추가 위험: ragas는 `instructor`로 구조화 출력을 강제하는데 `mlx_lm.server`의
  지원 여부가 미확인. 안 되면 우회 구현에 시간이 든다
- 로컬은 원래 계획대로 **생성 평가**에 쓴다(반복 횟수가 적고 처리량 부담이 작음)

**D15. 자체 지표를 버리지 않고 병행**
- RAGAS = 보고·판정용 정식 지표. 자체 지표 = 개발 중 몇 초 만에 도는 빠른 확인용
- 다만 자체 지표의 분모 버그(uncovered 6문항 포함)는 남아 있다. 계속 쓸 거면 고칠 것

**D16. LangChain 전면 전환은 하지 않기로**
- 사용자가 "직접 구현하다 보니 코드가 지저분해지는 것 같다"며 전환을 검토
- 실측: 파싱 계층 10,038줄(HWP 체인·품질 게이트·immutable run)은 LangChain이 대체 못 함.
  테스트 9,349줄도 폐기 대상. 총 27,969줄
- 실제 지저분한 곳은 프레임워크 부재가 아니라 **`search_api.py` 3,060줄 단일 파일**.
  LangChain으로 가도 이 파일은 그대로 남는다
- 남은 우선순위 1~5번은 전부 작고 국소적인 변경이라 프레임워크 이득이 없다
- 리팩터링(`search_api.py` 분리)은 실험 한 사이클 뒤로 미룸
- 단 **RAGAS는 파이프라인과 무관하게 도입 가능**하므로 그대로 진행 (실제로 그렇게 됨)

### 다음 단계
우선순위 1~5번. 전부 RAGAS context recall로 판정한다.
1. Lost in the Middle 정렬 · 2. 후보 30→50~100 · 3. parent-child ·
4. cross-encoder 리랭커 · 5. Contextual Retrieval (표 청크 373개 선택 적용)

### RAGAS 기준선 — 파서 프로필 비교 (53문항)

| | cascade (기존) | **challenger (채택)** |
|---|---|---|
| context recall 평균 | 0.4062 | **0.4522** (+11.3%) |
| 중앙값 | 0.3333 | **0.5000** |
| 1.0 (완전 뒷받침) | 15문항 | 16문항 |
| 0.0 (근거 없음) | 25문항 | **21문항** |

문항별로 **개선 8 / 악화 4 / 동일 41**. 크게 오른 것은 grant_052·027·002(전부 0 → 1.0),
떨어진 것은 grant_012(1.0 → 0.25), grant_035(1.0 → 0.5), grant_021(0.5 → 0), grant_018(0.833 → 0.667).

**표준 지표로 바꾼 실익이 여기서 나왔다.** 자체 지표로는 12 vs 10으로 challenger 우위만
보였는데, RAGAS는 주장 단위 부분 점수라 **challenger가 잃은 근거 4건이 드러난다.**
순증이 명확해 채택 판단은 유지하되, grant_012·035는 나중에 원인을 볼 것.

**점수 분포(challenger)**: 1.0이 16문항 / 부분 점수 15문항 / **0.0이 21문항**.
0.0은 근거가 컨텍스트에 하나도 없다는 뜻이므로 **생성 모델을 아무리 바꿔도 답할 수 없다.**
개선 여지가 검색 쪽에 있다는 근거이며, 우선순위 1~5번을 검색에 몰아둔 이유다.

주의: 53문항 중 7문항은 corpus 미수집이라 구조적으로 0점이고, grant_039처럼 기준답안이
"정확히 5일이면 기관에 확인하는 것이 안전하다" 같은 **판단·주의사항**을 담은 경우도 있어
원리적으로 근거를 찾을 수 없다. **1.0은 현실적 목표가 아니다.**

### 채점 경로 검토 — 구독 CLI와 로컬 모델 (2026-08-04 밤)

사용자가 구독 중인 Codex/Claude CLI와 로컬 모델로 채점을 돌릴 수 있는지 검토했다.

**구독 CLI (codex exec / claude -p) — 기각**
- 둘 다 설치돼 있고 호출은 된다(`claude -p`가 5.5초에 JSON 정상 출력). 단 **API 키가 아니라
  구독 인증**이라 OpenAI 호환 HTTP 엔드포인트가 아니다
- ragas는 `instructor`로 **엔드포인트 + 구조화 출력**을 요구하므로 CLI는 못 끼운다.
  쓰려면 RAGAS 프롬프트·알고리즘을 재구현해야 하는데, 그러면 "검증된 표준을 쓴다"는
  결정이 되돌아간다
- 속도 이득도 작다. 빈 프롬프트가 5.5초인데 문항당 12,000자를 넣으면 더 걸리고,
  매 호출마다 프로세스가 뜬다. 게다가 개발용 구독 한도를 채점에 소모하게 된다

**로컬 MLX — 동작하지만 4.5배 느림**

CLI와 달리 `mlx_lm.server`는 **OpenAI 호환 엔드포인트라 RAGAS가 그대로 붙는다.**
D14에서 "미확인"으로 남겼던 우려는 해소됐다. 단 모델이 갈린다:

| 모델 | 구조화 출력 |
|---|---|
| gemma-4-26b-a4b | ❌ max_tokens 8,192에서도 잘림. 스키마를 간결하게 못 지킴 |
| **Qwen3.6-35B-A3B** | ✅ 정상 |

| | 문항당 | 53문항 |
|---|---|---|
| Gemini (동시 6) | **10.8초** | 약 10분 |
| 로컬 Qwen | 48.4초 | 약 43분 |

**로컬은 동시 실행으로 못 줄인다** — GPU가 하나라 병렬로 돌려도 총 처리량이 같다.
Gemini가 동시 실행으로 얻은 3.2배가 로컬엔 없다. 원인은 문항당 12,000자(≈6,000토큰) prefill.

판정 일치도는 4건 중 3건 완전 일치(grant_002·003·004), 1건 불일치(grant_001: 로컬 1.0 vs
Gemini 0.5). 표본이 작지만 **판정 품질 자체는 쓸 만하다.**

**결정 기록**

**D17. 반복 측정용 정식 지표는 Gemini 유지, 로컬은 판정자 검증용**
- 근거: 개선안 1~5번마다 재측정하는데 43분 vs 10분이면 5회에 3.5시간 차이. 실험 속도가
  곧 생산성이다
- 로컬의 두 용도: (a) 20문항 독립 채점 → Gemini와 일치율 측정(비용 0, 백그라운드 가능,
  팀원 분담 2번의 kappa 검증과 같은 성격) (b) Gemini 무료 한도 소진 시 대체 경로
- 로컬로 갈 경우 **모델은 gemma가 아니라 Qwen3.6-35B-A3B**여야 한다(스키마 준수)
- D14에서 "mlx_lm.server의 구조화 출력 지원 미확인"이라고 적은 부분을 실측으로 해소.
  기각 사유는 지원 여부가 아니라 **처리량**으로 정정한다

## 2026-08-04 저녁 — 파라미터 스윕과 판정자 검증 (무인 실행)

RAG 입문서 49p의 "후보 50~100 → LLM엔 3~8" 권장값을 우리 corpus에서 검증했다.
`export_grant_contexts.py`에 이미 `--candidates`/`--top-k`가 있어 코드 변경 없이 스윕만 돌렸다.

### 할당량 절감 — `--carry-from`

`c100-k8`을 뽑아 보니 **53문항 중 40문항의 컨텍스트가 기준선과 완전히 동일**했다.
라우터가 스코프 필터를 걸고 상위 8개를 뽑는 구조라 후보를 30→100으로 늘려도 상위권이
거의 안 바뀐다. 그대로 채점하면 일일 한도만 태운다.

그래서 **컨텍스트 집합이 같은 문항은 기준선 점수를 그대로 옮기는** `--carry-from-samples`
/`--carry-from-scores`를 추가했다. context recall은 순서를 보지 않으므로 집합 비교로
충분하고, 근사가 아니라 정확한 절감이다. 스윕 전체가 약 320회 → **130회**로 줄었다.
`CallCounter`도 넣어 실행마다 소진량이 로그에 남는다(RPM뿐 아니라 RPD가 있고, 503 재시도가
그 한도를 함께 태우므로 문항 수가 아니라 호출 수를 세야 한다).

### 스윕 결과 — 어느 것도 기준선을 못 넘음

| 구성 | 후보 | top-k | context recall | 1.0 | 0.0 | API 호출 |
|---|---|---|---|---|---|---|
| **기준선** | 30 | 8 | **0.4522** | 16 | 21 | — |
| c100-k8 | 100 | 8 | 0.4094 | 14 | 23 | 13 |
| c30-k12 | 30 | 12 | 0.4434 | 17 | 22 | 49 |
| c100-k12 | 100 | 12 | 0.4570 | 17 | 21 | 68 |

### 그런데 이 표를 액면대로 읽으면 안 된다 — 판정자가 Lost in the Middle에 걸린다

`c30-k12`는 기준선 컨텍스트를 **53/53 전부 포함**하면서 청크를 더 넣은 구성이다.
근거가 늘기만 했는데 점수가 내려간 건 성립할 수 없다. 파고들었더니:

- 두 구성에서 **같은 6문항**이 하락(grant_002·010·013·014·024·053) — 무작위면 매번 다른
  문항이 흔들려야 한다
- `grant_002`는 컨텍스트가 커질수록 **단조 감소**: 1.00 → 0.71 → 0.57

**재현성 실험으로 확정**(같은 컨텍스트로 6문항 재채점, 12회 호출):

| 문항 | 1차(8청크) | 재실행(8청크) | 12청크 |
|---|---|---|---|
| grant_002 | 1.000 | **1.000** | 0.714 |
| grant_010 | 0.500 | **0.500** | 0.000 |
| grant_013 | 0.400 | **0.400** | 0.200 |
| grant_014 | 0.600 | **0.600** | 0.000 |
| grant_024 | 0.750 | **0.750** | 0.250 |
| grant_053 | 0.500 | 0.571 | 0.167 |

- **같은 입력 재실행 평균 절대편차 0.012** (6문항 중 5문항 완전 일치)
- **12청크로 늘렸을 때 평균 낙폭 0.403**

**노이즈보다 33배 큰 체계적 효과다.** 판정자는 재현성이 매우 높은데, 컨텍스트가 길어지면
분명히 있는 근거를 못 찾는다.

**함의 4가지:**
1. 스윕 결과는 "컨텍스트를 키웠더니 검색이 나빠졌다"가 아니라 **"판정자가 근거를 못 찾게
   됐다"**이다. 실제 검색 품질은 오히려 좋아졌을 수 있다
2. 입문서 59p Lost in the Middle이 **판정자에게도 적용**된다. 같은 효과가 우리 **생성
   모델에도 일어날 것**이다 — 컨텍스트를 키우면 근거는 늘지만 LLM이 못 읽는다
3. 그래서 **우선순위 1번(정렬)을 뒤가 아니라 앞으로 당겨야 한다.** "정렬은 context recall로
   측정이 안 되니 뒤로"라고 판단했었는데, 이 결과는 **컨텍스트를 키우는 어떤 개선도 정렬
   없이는 반쪽**임을 보여준다
4. 지표 자체는 신뢰할 만하다(재현성 0.012). 단 **컨텍스트 크기가 다른 구성끼리 비교할 때는
   이 편향을 감안**해야 한다. 같은 크기끼리(예: parent-child 8청크 vs 기준선 8청크) 비교는 문제없다

### 판정자 검증 — 로컬 Qwen vs Gemini (동일 53문항, 비용 0)

| | Gemini | 로컬 Qwen3.6 |
|---|---|---|
| context recall 평균 | 0.4522 | 0.4871 |
| 1.0 문항 | 16 | 22 |
| 0.0 문항 | 21 | 21 |

- 평균 절대편차 **0.090**, 완전 일치 39/53, 0.2 이내 42/53
- **이진 판정(근거 있음/없음) 일치율 0.962, Cohen's kappa 0.921** — 매우 높은 일치
- 크게 갈린 문항은 전부 같은 패턴: **Gemini 0.50 vs Qwen 1.00**
  (grant_001·007·015·020·035). Qwen이 부분 근거를 더 관대하게 인정한다

**결론: "근거가 있나 없나"는 두 판정자가 사실상 같은 답을 준다(kappa 0.921).
차이는 부분 인정 기준의 엄격도다.** Gemini가 더 보수적이므로 개선폭을 과대평가할
위험이 낮다 — 기준 판정자로 유지하는 게 맞다.

이건 팀원 분담 계획 2번(사람 채점 + LLM judge 일치율 kappa)의 LLM 쪽 절반에 해당한다.
사람 채점을 추가하면 3자 비교가 된다.

### 결정 기록 (8/4 저녁)

**D18. `--carry-from`으로 일일 한도 절감**
- 개선안마다 재측정하는데 컨텍스트가 안 바뀐 문항까지 매번 채점하면 한도를 태운다
- context recall이 순서 불변이라 집합 비교로 정확히 판정 가능. 근사가 아님
- 앞으로 parent-child·리랭커 실험에서도 상당수 문항이 안 바뀔 것이라 계속 유효

**D19. 우선순위 1번(Lost in the Middle 정렬)을 앞으로 당김**
- 원래 "context recall이 순서를 안 보니 측정 불가 → 뒤로"였다
- 그런데 **판정자 자신이 긴 컨텍스트에서 근거를 놓치는 것을 실측**했다(낙폭 0.403 vs
  노이즈 0.012). 생성 모델도 같은 취약점을 가질 것이므로, 컨텍스트를 키우는 개선은
  정렬과 묶어야 의미가 있다
- 측정은 여전히 생성 평가로만 가능. 그래서 **1번 + 생성 평가를 한 묶음**으로 진행할 것

**D20. 기준 판정자는 Gemini 유지 (kappa 0.921로 뒷받침)**
- 로컬 Qwen과 이진 판정이 96.2% 일치. 지표 신뢰도에 대한 독립 근거 확보
- Gemini가 부분 인정에 더 보수적 → 개선폭 과대평가 위험이 낮음
- 로컬은 비용 0이므로 앞으로도 주요 마일스톤마다 교차 검증용으로 돌릴 것

## 미해결/주의
- Drive 자료실 접근 요청은 **보내지 않음** (사용자 결정 대기 상태였음 → 공식 수집으로 대체)
- 수과원은 2026판 사용 중 — 기준답안(2025 기반)과 단가 다를 수 있음
- 53문항 원본 `연구비 Q&A(연구비관리부)_25.8.1..hwp`를 corpus에 넣으면 평가가
  치팅이 되므로 **eval corpus에서 제외** 유지할 것
