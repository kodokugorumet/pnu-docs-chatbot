# 성능 최적화 성과 보고 — 검색·생성 품질 개선 (2026-08-05 ~ 08-06)

> 상세 근거·측정 조건·기각 대안은 `worklog-20260803-grant-rules.md`의 D24~D33 참고.
> 교수님 요구 6번(타깃 확장보다 성능 최적화 집중)에 대한 이틀간의 결과 요약.

## 한 줄 요약

**검색 품질 +16.5%, 최종 답변 품질 +42%** — 표준 RAG 파이프라인(하이브리드 검색
→ 융합 → 리랭킹 → 부모 확장)을 단계별로 실측 검증하며 구축. 각 단계의 채택·기각
근거가 전부 수치로 남아 있다.

## 1. 핵심 수치

**검색 (RAGAS context recall, 53문항 평가셋)**

```
0.4522 ──→ 0.4644 ──→ 0.4877 ──→ 0.5157 ──→ 0.5266
기준선    +리랭커    +부모확장    +순위융합    +dense레인
(BM25)      (A)       (A+P)        (F)       (E-snow) ★채택
```

- 0점(근거 전무) 문항: 21 → 16
- 지표 허상 6문항(아래 §4) 감안 시 실질 ~0.57

**생성 (LLM judge 0~2점, 53문항)** — 검색 개선의 답변 품질 전이 확증(D32):

| | 기준 검색 | 채택 파이프라인 | |
|---|---|---|---|
| 평균 | 0.81 | **1.15** | **+42%** |
| 완전 정답(2점) | 13 | **23** | +10문항 |
| 오답형(환각 위험) | 3 | 3 | **증가 없음** |

검색이 살린 문항 = 생성이 살아난 문항(0→2점 회복 6개 중 4개 일치).
검색→생성 인과가 문항 단위로 연결된다.

## 2. 채택 파이프라인 (D31+D32)

```
질의 → BM25 후보 ∪ dense 후보(Snowflake-ko)     ← 리콜: 어휘 매칭 + 의미 매칭
     → RRF 병합 → bge cross-encoder 채점        ← 정밀도: 본문 정독 재채점
     → (검색순위 × CE순위) 재융합                ← CE 전횡 방지 (D30)
     → 문서당 2청크 → 기관 라우터 → 상위 8개
     → section_path 부모 확장 (조문+별표 재결합)  ← 완성도 (D29)
     → LitM 가장자리 배치 → LLM 생성             ← D24 무검증 기본값
```

## 3. 검증하고 기각한 것들

| 시도 | 결과 | 교훈 |
|---|---|---|
| CE 리랭커 단독 교체 (A) | +0.012에 그침 | 새 근거를 찾는 만큼 BM25 직격 매칭을 밀어냄 → 융합 필요 |
| 생성형 리랭커 Qwen3-0.6B (B) | 판별형에 패배 (0.4248) | 함정: seq-cls 변환판도 전용 템플릿 필수, 누락 시 0.1386으로 붕괴 (D26) |
| 문서당 캡 완화 단독 (C) | 0.3954 역효과 | 조각 선택기 없이 슬롯만 늘리면 장황한 조각이 독식 (D28) |
| 부모 확장 단독 (P) | 0.4364 | 잘못 고른 조각을 확장하면 잘못된 섹션만 커짐 → 리랭커 뒤에서만 유효 (D29) |
| LitM 정렬 단독 실험 | 실행 안 함 | 8청크에선 효과 없음이 데이터 함의 (재현성 0.012 vs 12청크 낙폭 0.403) → 기본값으로만 (D24) |
| KURE-v1 dense | 평균 동률, 꼬리 열세 | 0점 문항 2개 더 많아 Snowflake-ko 채택 (D31) |

## 4. 남은 실패의 성격 (D33) — 다음 투자처

E-snow 0점 16문항 전수 분류:

- **6문항 = 지표 허상**: 같은 컨텍스트로 생성은 1~2점. RAGAS 문장귀속 한계
- **6문항 = 수집 문제**: 미수집 3(극지연구소 세부기준·선박플랜트 편성기준) +
  여비 유권해석·지침 부재 3(규정 본문은 있으나 답의 출처가 해석례)
- **4문항 = 검색 개선 여지**: 017·021·028·031

→ **검색 튜닝의 한계효용 < 수집.** 다음 확실한 +6문항은 문서 수집에 있다.

## 5. 재사용 가능한 평가 인프라 (부산물)

- 검색 1회 export → 채점 N회 분리 구조를 생성 평가까지 이식
  (`evaluate_grant_generation.py --contexts/--litm-order/--rejudge`)
- carry-from으로 채점 비용 절감, 판정자 교차검증(로컬 Qwen kappa 0.921)
- 운영 노하우: Gemini 한도는 모델별 분리·midnight PT(=한국 16시) 리셋,
  429 백오프 재시도가 한도를 가속 소진 → 폴백은 다른 모델 풀로

## 6. 산출물

- 커밋 `05dfc02`~`d0ce2fb` (6건), 결정 기록 D24~D33
- 신규: `scripts/rag/cross_encoder.py`, `scripts/rag/parent_expand.py`,
  dense 아티팩트 `processed/index/learned-dense/grant-20260804-{snowflake-ko,kure-v1}`
- 대기: flash-lite 공식 생성 트랙 (16시 자동 완료, 8/3 참고치 0.67과 직접 비교용)
